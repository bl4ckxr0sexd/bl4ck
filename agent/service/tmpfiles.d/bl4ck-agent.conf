# /run/bl4ck is tmpfs-backed and wiped on reboot. The systemd unit uses
# ProtectSystem=strict + ReadWritePaths=/var/run/bl4ck, which fails sandbox
# setup if the directory is missing. This snippet recreates it on every boot
# before services start.
d /run/bl4ck 0770 root breeze -
